using System.Text;
static class X
{
	public static string Ack(string s) => s + "!";
	public static steing SayHello(StringBuilder b)
	{
		var names = new[] { 
			"Holly", "Molly"
		};
		for (var i = 0; i < names::it.Count; ++i)
			(i > 0 ? b.Append("Hello ").Append(", ") : b.Append("Hello ")).Append(Ack(it[i]));
		b.Append("Hello ").Append(" here");
		return b.ToString();
	}
}